services:
  gateway-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data:ro
      - ./predictions.csv:/app/predictions.csv
    environment:
      - PYTHONUNBUFFERED=1